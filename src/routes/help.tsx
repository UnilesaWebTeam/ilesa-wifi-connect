import { createFileRoute } from "@tanstack/react-router";
import { AlertTriangle, KeyRound, LogIn, WifiOff } from "lucide-react";
import { PageHeading, PortalShell } from "@/components/portal";
import { Button } from "@/components/ui/button";
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
import { toast } from "sonner";

export const Route = createFileRoute("/help")({ head: () => ({ meta: [{ title: "Help — University of Ilesa Wi-Fi" }, { name: "description", content: "Get help with your university Wi-Fi account and connection." }, { property: "og:title", content: "University Wi-Fi Help" }, { property: "og:description", content: "Guidance for University of Ilesa Wi-Fi account and connection issues." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: HelpPage });
function HelpPage() { const items = [{value:"login",icon:LogIn,title:"I can’t log in",body:"Confirm that you selected the correct account type and entered your matriculation number or Staff ID exactly as issued. For security, sign-in errors do not confirm whether an ID exists."},{value:"password",icon:KeyRound,title:"I forgot my password",body:"Request a password reset using your university email address. The reset link will only be sent to the verified email on your account.",action:"Reset password"},{value:"wifi",icon:WifiOff,title:"My Wi-Fi credentials are not working",body:"Check that you selected University-WiFi and entered both values exactly. If the issue continues, contact the university ICT and network support desk."},{value:"compromised",icon:AlertTriangle,title:"Someone else has my Wi-Fi credentials",body:"Stop using the credential and report it immediately. The ICT team can revoke it and issue a replacement.",action:"Report compromised credential"}]; return <PortalShell><PageHeading title="Need help?" description="Find answers to common account and Wi-Fi connection questions." /><section className="rounded-xl border bg-card px-5 shadow-soft sm:px-7"><Accordion type="single" collapsible>{items.map(({value,icon:Icon,title,body,action}) => <AccordionItem key={value} value={value}><AccordionTrigger className="py-5 text-left hover:no-underline"><span className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg bg-primary/8 text-primary"><Icon className="size-4" /></span>{title}</span></AccordionTrigger><AccordionContent><div className="pb-5 pl-12"><p className="max-w-2xl text-sm leading-6 text-muted-foreground">{body}</p>{action && <Button variant="outline" size="sm" className="mt-4" onClick={() => toast.info("Support action preview", { description: "This action will connect to university support in a future release." })}>{action}</Button>}</div></AccordionContent></AccordionItem>)}</Accordion></section><div className="mt-6 rounded-xl bg-primary p-6 text-primary-foreground"><h2 className="text-lg font-semibold">Still need assistance?</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-primary-foreground/80">Visit the university ICT support desk with your university ID. Never send your Wi-Fi password by email or chat.</p></div></PortalShell>; }