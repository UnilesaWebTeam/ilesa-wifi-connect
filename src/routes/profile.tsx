import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { KeyRound, LogOut } from "lucide-react";
import { PageHeading, PortalShell, StatusBadge } from "@/components/portal";
import { Button } from "@/components/ui/button";
import { students } from "@/lib/mock-portal";

export const Route = createFileRoute("/profile")({ head: () => ({ meta: [{ title: "My Profile — University of Ilesa Wi-Fi" }, { name: "description", content: "Review your verified university account information." }, { property: "og:title", content: "My University Wi-Fi Profile" }, { property: "og:description", content: "Verified University of Ilesa Wi-Fi account information." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ProfilePage });
function ProfilePage() { const user = students.student; const navigate = useNavigate(); return <PortalShell><PageHeading title="My Profile" description="Your verified university account information." /><section className="overflow-hidden rounded-xl border bg-card shadow-soft"><div className="flex items-center gap-4 border-b p-5 sm:p-7"><span className="grid size-14 place-items-center rounded-full bg-primary/10 text-lg font-bold text-primary">IS</span><div><h2 className="text-lg font-semibold">{user.name}</h2><p className="text-sm text-muted-foreground">{user.email}</p></div><div className="ml-auto hidden sm:block"><StatusBadge /></div></div><dl className="grid sm:grid-cols-2">{[["Name",user.name],["University ID",user.universityId],["University email",user.email],["Department",user.department],["Faculty / Unit",user.faculty],["Account type","Student"]].map(([label,value]) => <div key={label} className="border-b px-5 py-4 last:border-b-0 sm:px-7 sm:even:border-l"><dt className="text-xs font-medium text-muted-foreground">{label}</dt><dd className="mt-1 text-sm font-semibold">{value}</dd></div>)}</dl><div className="flex flex-col gap-3 bg-muted/40 p-5 sm:flex-row sm:px-7"><Button variant="outline"><KeyRound /> Change password</Button><Button variant="outline" className="text-destructive" onClick={() => void navigate({ to: "/" })}><LogOut /> Sign out</Button></div></section></PortalShell>; }
